{
  "openapi": "3.0.0",
  "info": {
    "title": "Webhook Receiver API",
    "version": "1.0.0",
    "description": "Auto-generated OpenAPI documentation. Markdown supported in descriptions (CommonMark)."
  },
  "paths": {
    "/receive_order_webhook": {
      "get": {
        "summary": "Receive order webhook",
        "description": "Accepts an inbound webhook event for asynchronous processing.\n\nIf `WEBHOOK_SECRET` is set, the `X-Signature` header is verified using HMAC-SHA256 before the payload is accepted.",
        "operationId": "get_receive_order_webhook",
        "tags": [
          "webhooks"
        ],
        "responses": {
          "202": {
            "description": "Webhook accepted for processing",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WebhookAcceptedResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request payload"
          },
          "401": {
            "description": "Invalid webhook signature"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "WebhookAcceptedResponse": {
        "properties": {
          "delivery_id": {
            "description": "Unique delivery identifier.",
            "title": "Delivery Id",
            "type": "string"
          },
          "status": {
            "default": "accepted",
            "description": "Processing status.",
            "title": "Status",
            "type": "string"
          },
          "received_at": {
            "description": "ISO-8601 timestamp of receipt.",
            "title": "Received At",
            "type": "string"
          }
        },
        "required": [
          "delivery_id",
          "received_at"
        ],
        "title": "WebhookAcceptedResponse",
        "type": "object"
      }
    }
  }
}
